#!/bin/sh

set -e

. /usr/share/debconf/confmodule

if [ "$DEBCONF_FRONTEND" != "noninteractive" ] && [ -x /usr/bin/wget -o -x /usr/bin/curl ]; then
	db_input high rt73-fwcutter/cut_firmware || true
	db_go || true
fi

