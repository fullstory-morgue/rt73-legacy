#!/bin/sh

set -e

. /usr/share/debconf/confmodule

db_get rt73-fwcutter/cut_firmware
if [ "$RET" = "true" ]; then
	if [ ! -e /lib/firmware/rt73.bin ]; then
		/usr/share/rt73-fwcutter/install_rt73_firmware.sh
	fi
fi

#DEBHELPER#

